class Entry {
  String name;     // SANS
  String place;    // L.A
  int done;        // 0
  int total;       // 4

  Entry({
    required this.name,
    required this.place,
    required this.done,
    required this.total,
  });
}